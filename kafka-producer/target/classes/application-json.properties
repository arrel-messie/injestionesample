# Kafka Configuration for JSON Producer
kafka.bootstrap.servers=localhost:9092
kafka.topic.name=TransactionsJSON
kafka.client.id=transaction-json-producer

# Producer Configuration
producer.acks=all
producer.retries=3
producer.batch.size=16384
producer.linger.ms=10
producer.buffer.memory=33554432
producer.compression.type=snappy
producer.enable.idempotence=true
producer.max.in.flight.requests.per.connection=5

# Producer Behavior
producer.send.interval.seconds=30
producer.send.count=-1
producer.random.data.enabled=true

# Data Generation Configuration
data.montant.min=10.0
data.montant.max=1000.0
data.commande.id.prefix=CMD-

# Logging
logging.level.root=INFO
logging.level.com.kafka.injector=DEBUG




